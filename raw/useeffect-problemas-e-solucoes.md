# useEffect — Problemas, Armadilhas e Soluções

## Transcrição

11 linhas três us state dois uses effect você consegue ver o problema três renderizações por mudança agora três linhas um use state nenhum effect uma só renderização o mesmo resultado e provavelmente metade dos seus componentes têm esse problema o use effect foi feito para efeitos colaterais ou seja coisas que acontecem fora do React chamadas API event listeners timers mas a maioria ainda usa o use effect para sincronizar estado quando X mudar atualiza Y parece fazer sentido mas é aí que os bugs aparecem voltando pro código exemplo do início quando itens muda o React renderiza depois o primeiro User effect roda e seta filtrado isso causa outra renderização aí o segundo use effect roda e seta total mais uma renderização três renderizações e entre o primeiro e o terceiro o componente tá num estado inconsistente itens já mudou mas total ainda é o valor antigo a correção é bem simples filtrado e total não são estado são valores derivados você consegue calcular eles a partir de itens então calcula durante a renderização isso sem os effect sem os state estraer uma renderização só que é impossível ficar inconsistente porque tudo é calculado junto a regra é essa se dá para calcular a partir de estado ou props existentes não coloca no use state mas e se eu calcular o estado durante a renderização não corre o risco de ficar lento essa é uma pergunta bem comum e na maioria dos casos não filtrar 1000 itens leva menos de 1 milissundo overhead duas renderizações extras com effect mas se o cálculo for realmente pesado e você mediu isso com profiler por exemplo aí sim você usa o useo não use effect com use state o useo faz o cálculo durante a renderização sem uma renderização extra é uma diferença pequena mas importante o use mesmo calcula e retorna na mesma renderização o use effect com use state agenda uma segunda renderização o exemplo do início tinha dois effect em cadeia mas por exemplo em produção eu já vi componentes com cinco ou seis efects encadeados quatro state três use effect quatro renderizações em cadeia e o pior é que se você muda a lógica de permissions por exemplo você precisa rastrear o impacto nos efectos seguintes e isso é bem ruim porque gera um efeito dominó cada peça depende da anterior e qualquer mudança pode quebrar toda a cadeia e agora com quatro linhas um use state e nenhum effect uma renderização e a lógica fica legível porque é sequencial de cima para baixo sem pular entre efectos agora eu vou mostrar uma consequência prática que a maioria dos devs não entende ainda de usar muitos use effects esse counter nunca passa de um o use effect com array vazio captura o count da primeira renderização que é zero e esse valor fica congelado dentro da closure para sempre a closure captura variáveis não valores mas como cada renderização cria novas variáveis a closure da primeira renderização só vê as variáveis da primeira renderização para corrigir isso é só usar o updater function do set state em vez de ler count da closure você pede pro React te dar o valor mais recente mas um detalhe importante desse bug é que ele só existe porque tem os effect seu seu código não dependesse de effects não teria esse problema caso queira saber mais detalhes sobre closures tem um vídeo sobre fundamentos de JavaScript aqui no canal agora vamos falar do uso mais popular do User Effect que é buscar dados esse código é tão comum você provavelmente já deve ter visto que parece certo mas dentro dele tem três bugs escondidos o bug número um é a race condition se user ID muda rápido as respostas podem voltar fora de ordem você clica no usuário dois mas a resposta do usuário um chega por último e o componente mostra os dados errados o bug número dois é: se o componente desmonta antes da resposta chegar o set user tenta atualizar um componente que não existe mais e o bug número três é entre mudar o user ID e a resposta chegar o componente mostra os dados do usuário anterior sem estado de loading sem tratamento de erro essa versão corrigida usa board controller para cancelar requests antigos no cleanup isso resolve a race condition e o vazamento de memória mas olha quanta coisa que teve que ser escrita 30 linhas para fazer um fat com tratamento correto agora um dos pontos principais desse vídeo você não deveria estar fazendo fat no effect no exemplo desse código a race condition tá tratada cash tratado retry tratado também loading erro também revalidação automática também tratado cinco linhas em vez de 30 e nenhum use effect bibliotecas como TCK Query SWR ou até os hooks de data fating do NextJS e do Remix resolvem todos esses problemas ela existem especificamente porque o fat é tão difícil de fazer direito que virou uma categoria de bug específica mas deixando claro que use effect não é errado temos alguns usos legítimos por exemplo sincronizar com sistemas externos ao React event listness no window ou document subscriptions a web sockets integração com bibliotecas que manipulam diretamente timers e intervalos com cleanup correto analytics login etc a regra é se dá para calcular a partir de estado ou props calcula na renderização se é fat de dados usa uma biblioteca de data fet especializada se é um efeito colateral external react aí sim você usa o use effect fazendo um resumo agora estado derivado se dá para calcular calcula na renderização não usa o use effect para sincronizar estado cadeia de effects se um use effect seta estado que dispara outro use effect provavelmente ambos deveriam ser cálculos diretos e o data fating effect conf tem race condition vazamento de memória e estados inconsistentes usa o tank stack query ou similar o melhores effect é o que você deleta e é isso se curtiu se inscreve e deixa um like que isso ajuda bastante o canal
